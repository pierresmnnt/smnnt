{% extends 'base.html.twig' %}

{% block title %}Manage Categories{% endblock %}

{% block page__title %}Manage Categories{% endblock %}

{% block page__desc %}
    <p>Currently {{ categories|length }} categories</p>
    <a href="{{ path('category_new') }}" class="button button-primary">{{ icon('add') }}<span class="icon-label ">New Category</span></a>
{% endblock %}

{% block page__content %}
    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Type</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for category in categories %}
            <tr>
                <td>{{ category.id }}</td>
                <td>{{ category.name }}</td>
                <td>{{ category.typeName }}</td>
                <td>
                    <a href="{{ path('category_edit', {'id': category.id}) }}" class="button">{{ icon('edit') }}</a>
                    {{ include('admin/category/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
