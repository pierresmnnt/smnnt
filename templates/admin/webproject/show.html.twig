{% extends 'base.html.twig' %}

{% block title %}WebProject{% endblock %}

{% block body %}
    <h1>WebProject</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ web_project.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ web_project.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ web_project.description }}</td>
            </tr>
            <tr>
                <th>Link</th>
                <td>{{ web_project.link }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_web_project_admin_index') }}">back to list</a>

    <a href="{{ path('app_web_project_admin_edit', {'id': web_project.id}) }}">edit</a>

    {{ include('web_project_admin/_delete_form.html.twig') }}
{% endblock %}
