<form method="post" action="{{ path('admin_portfolio_delete', {'id': image.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="delete-form">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ image.id) }}">
    <div class="form-submit">
        <button type="submit">&#128465;</button>
    </div>
</form>
