{% extends 'base.html.twig' %}

{% block title %}Adslot{% endblock %}

{% block body %}
    <h1>Adslot</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adslot.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ adslot.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_adslot_index') }}">back to list</a>

    <a href="{{ path('app_adslot_edit', {'id': adslot.id}) }}">edit</a>

    {{ include('admin/adslot/_delete_form.html.twig') }}
{% endblock %}
