<nav class="navbar">
    <div class="navbar-toggler">
        <button type="button" data-bs-toggle="collapse" data-bs-target="#js-nav" aria-controls="js-nav" aria-expanded="false" aria-label="Toggle navigation" id="js-toggleBtn">{{ icon('menu') }}</button>
    </div>
    <div class="navbar-brand">
        <a href={{ path("default") }} class="navbar-site__brand">SMNNT</a>
    </div>
    <div class="navbar-nav-wrapper" id="js-nav">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('portfolio_index') }}" {{ menu('portfolio') }}>Photos</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('article_index') }}" {{ menu('articles') }}>Articles</a>
            </li>
        </ul>
    </div>
    <div class="navbar-auth">
        {% if app.user %}
            <a class="nav-link" href="{{ path("admin") }}" {{ menu('admin') }}>Admin</a>
        {% else %}
            <a class="nav-link" href="{{ path("login") }}">Login</a>
        {% endif %}
    </div>
</nav>